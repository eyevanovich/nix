# glab 1.108 JSON fixtures

Representative `glab repo view --output json` and `glab issue view --output json`
shapes used by the fake-executor tests. Identifiers and host names are synthetic;
no authentication data is captured.
